task: 'retriever'

env:
  num_threads: 16
  seed: 42

dataset:
  name: 'medmcqa_primekg'
  text_encoder_name: 'gte-large-en-v1.5'

retriever:
  topic_pe: true
  DDE_kwargs:
    num_rounds: 2
    num_reverse_rounds: 2

optimizer:
  lr: 1e-3

eval:
  k_list: '50,100,200'

train:
  num_epochs: 200
  patience: 10
  save_prefix: 'medmcqa_primekg'
